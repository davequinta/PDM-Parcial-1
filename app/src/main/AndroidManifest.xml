<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="quintanilla00025815.pm_parcial_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QuizActivity" />
        <activity android:name=".Finalgame" />
        <activity android:name=".MainEdit" />
        <activity
            android:name=".CountryListActivity"
            android:label="@string/title_country_list"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="quintanilla00025815.pm_parcial_1.MainActivity" />
        </activity>
        <activity
            android:name=".CountryDetailActivity"
            android:label="@string/title_country_detail"
            android:parentActivityName=".CountryListActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="quintanilla00025815.pm_parcial_1.CountryListActivity" />
        </activity>
        <activity android:name=".addingActivity"></activity>
    </application>

</manifest>